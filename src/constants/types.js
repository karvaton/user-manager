const layer = {
    ADD: 'layer/add',
    CHANGE: 'layer/change',
    REMOVE: 'layer/remove',
    SET: 'layer/set',
}

const app = {
    ERROR: 'app/error',
}

const user = {
    SET_LAYERS: 'user/set_layers',
    DELETE: 'user/delete',
    SET: 'user/set',
}

const layers = {
    CHANGE_ORDER: "layers/change_order",
};

export {
    app,
    user,
    layer,
    layers,
}