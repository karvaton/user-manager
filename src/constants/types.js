const layer = {
    ADD: 'layer/add',
    CHANGE: 'layer/change',
    REMOVE: 'layer/remove',
}

const app = {
    ERROR: 'app/error',
}

const user = {
    DELETE: 'user/delete',
}

export {
    app,
    user,
    layer,
}