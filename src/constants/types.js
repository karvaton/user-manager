const layer = {
    ADD: 'layer/add',
    CHANGE: 'layer/change',
    REMOVE: 'layer/remove',
    SET: 'layer/set',
}

const app = {
    ERROR: 'app/error',
}

const user = {
    SET_LAYERS: 'layers/set_layers',
    DELETE: 'user/delete',
    SET: 'user/set',
}

export {
    app,
    user,
    layer,
}