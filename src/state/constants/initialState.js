const initialState = {
    userManager: {
        users: [],
        modalWindow: null,
    },
    registration: {
        layers: [],
        loading: false,
        activeLayer: null,
        entry: {},
    },
};

export default initialState;